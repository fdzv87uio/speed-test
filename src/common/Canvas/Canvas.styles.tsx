import Styled from 'styled-components'

export const Cross = Styled.img`

            position: absolute;
            margin-left: auto;
            margin-right: 'auto';
            top: 0;
            left: 0;
            z-index: 9;


`
